---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: keda-webhooks
  namespace: keda
  labels:
    app: keda-webhooks
    app.kubernetes.io/name: keda-webhooks
    app.kubernetes.io/version: latest
    app.kubernetes.io/component: webhooks
    app.kubernetes.io/part-of: keda-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keda-webhooks
  template:
    metadata:
      labels:
        app: keda-webhooks
        name: keda-webhooks
      name: keda-webhooks
    spec:
      securityContext:
        runAsNonRoot: true
      serviceAccountName: keda-operator
      containers:
        - name: keda-webhooks
          image: ghcr.io/kedacore/keda-webhooks:latest
          command:
            - /keda-webhooks
          args:
            - --zap-log-level=info
            - --zap-encoder=console
            - --zap-time-encoding=rfc3339
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 1000m
              memory: 1000Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 25
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 20
          ports:
          - containerPort: 9443
            name: http
            protocol: TCP
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: WATCH_NAMESPACE
              value: ""
            - name: KEDA_HTTP_DEFAULT_TIMEOUT
              value: ""
          securityContext:
            runAsNonRoot: true
            capabilities:
              drop:
              - ALL
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
          - mountPath: /certs
            name: cert
            readOnly: true
      terminationGracePeriodSeconds: 10
      nodeSelector:
        kubernetes.io/os: linux
      volumes:
      - name: cert
        secret:
          defaultMode: 420
          secretName: kedaorg-webhook-secret

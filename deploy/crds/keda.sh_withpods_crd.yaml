apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: withpods.keda.sh
spec:
  group: keda.sh
  names:
    kind: WithPod
    listKind: WithPodList
    plural: withpods
    singular: withpod
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WithPod is the shell that demonstrates how PodSpecable types
          wrap a PodSpec.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WithPodSpec is the shell around the PodSpecable within WithPod.
            properties:
              template:
                $ref: '#/definitions/k8s.io~1api~1core~1v1~0PodTemplateSpec'
                description: PodSpecable is implemented by types containing a PodTemplateSpec
                  in the manner of ReplicaSet, Deployment, DaemonSet, StatefulSet.
            type: object
        type: object
    served: true
    storage: true

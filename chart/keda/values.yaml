nameOverride:
fullnameOverride:

image:
  repository: kedacore/keda
  tag: latest
  pullPolicy: Always

replicaCount: 1

rbac:
  create: true

serviceAccount:
  create: true
  name:

logLevel: info

#set to a higher verbosity for detailed apiservice logs
glogLevel: 2
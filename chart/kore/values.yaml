nameOverride:
fullnameOverride:

image:
  repository: projectkore.azurecr.io/kore
  tag: master
  pullPolicy: Always

replicaCount: 1

rbac:
  create: true

serviceAccount:
  create: true
  name: kore-service-account

disableTLSVerification: false
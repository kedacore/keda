nameOverride:
fullnameOverride:

image:
  repository: korecr.azurecr.io/kore
  tag: master
  pullPolicy: Always

replicaCount: 1

rbac:
  create: true

serviceAccount:
  create: true
  name:

logLevel: info